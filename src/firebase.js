// ParkSpot uses localStorage — Firebase DB and Storage are not used.
// Auth is also stubbed since login/signup use security.js + storage.js.
// This file exists only to satisfy any residual imports.

export const auth = null;
export const db = null;
export const storage = null;